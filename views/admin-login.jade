div#admin-bg
	div.fg-login
		form(method='post', action='/admin', enctype='application/x-www-form-urlencoded')
			h3 Log in
			if prohibited
				.error Error! You are prohibited from accessing this area.
			if invalid
				.error Please enter a valid email address and password.
			div.form-group
				label(for='email') Email:
				input.form-control#email(name='email')
			div.form-group
				label(for='password') Password:
				input.form-control#password(name='password', type='password')
			button.btn.btn-lg.btn-primary(type='submit', value='Log In') Log In